<%! func createTable(w io.Writer, t reflect.Type) error %>

<%% import "reflect" %%>

CREATE TABLE <%= t.Name() %> (
  <% for i := 0; i < t.NumField(); i++ { %>
    <% field := t.Field(i) %>
    <%= field.Name %> <%= sqlType(field) %><% if i < t.NumField()-1 { %>,<% }%>
  <% } %>
);

